import 'package:flutter/material.dart';

class AgeingSlabWithPaymentTermPage extends StatelessWidget {
  const AgeingSlabWithPaymentTermPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Your Ageing Slab With Payment Term page UI implementation here
    return Scaffold(
      appBar: AppBar(
        title: Text('Ageing Slab With Payment Term'),
      ),
      body: Center(
        child: Text('Ageing Slab With Payment Term Page Content'),
      ),
    );
  }
}