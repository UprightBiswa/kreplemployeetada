import 'package:flutter/material.dart';

class AgeingSlabWithoutPaymentTermPage extends StatelessWidget {
  const AgeingSlabWithoutPaymentTermPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Your Ageing Slab Without Payment Term page UI implementation here
    return Scaffold(
      appBar: AppBar(
        title: Text('Ageing Slab Without Payment Term'),
      ),
      body: Center(
        child: Text('Ageing Slab Without Payment Term Page Content'),
      ),
    );
  }
}